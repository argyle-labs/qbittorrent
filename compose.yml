services:
  qbittorrent:
    image: ghcr.io/scottdkey/qbittorrent:latest
    container_name: qbittorrent
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /opt/qbittorrent/config:/config
